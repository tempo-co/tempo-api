# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Category {
  id: ID!
  name: String!
  description: String!
  transactions: [Transaction!]!
}

type User {
  id: ID!
  name: String!
  email: String!
  isActive: Boolean!
  createdDate: DateTime!
  accounts: [Account!]!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type Account {
  id: ID!
  alias: String!
  balance: Float!
  bank: Bank!
  user: User!
  transactions: [Transaction!]!
  bankStatements: [BankStatement!]!
}

"""Supported banks"""
enum Bank {
  ABN_AMRO
  REVOLUT
}

type BankStatement {
  id: ID!
  account: Account!
  transactions: [Transaction!]!
}

type Transaction {
  id: ID!
  startedDate: DateTime!
  completedDate: DateTime!
  description: String!
  amount: Float!
  currency: String!
  account: Account!
  bankStatement: BankStatement!
  category: Category!
}

type TypeAccessToken {
  access_token: String!
}

type Query {
  transactions: [Transaction!]!
  transaction(id: ID!): Transaction!
  users: [User!]!
  user(id: ID!): User!
}

type Mutation {
  logIn(email: String!, password: String!): TypeAccessToken!
  signUp(name: String!, email: String!, password: String!): TypeAccessToken!
  bankStatementUpload(file: Upload!, accountId: String!): BankStatement!
  accountCreate(alias: String!, bank: String!): Account!
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload